# Única Fuente de Verdad: Pendientes, Backlog y Deuda Técnica

Este archivo consolida todas las tareas pendientes, deudas técnicas y ajustes de UX para mantener el contexto unificado entre los agentes de IA y el desarrollador.

> **Nota (2026-07-07):** el dominio académico completo fue rediseñado a Bounded Contexts DDD (Carrera → PlanEstudio → MateriaPlan → Comisión → Cursada; CicloLectivo → PeriodoAcademico separado, no atado a una carrera). Todo lo listado en las secciones de abajo corresponde al modelo viejo y ya fue superado/completado. Ver `docs/02.00-Modelo_Datos.md` para el modelo vigente.

## 🎯 Pendientes actuales (post-refactor DDD)

1. **[HECHO] Correlatividades** — `MateriaPlan.correlativaIds` expuesto en API + checklist en Malla Curricular; `CursadaServiceImpl.guardar` rechaza (400) matricular sin la correlativa aprobada (`condicionFinal` matchea `PROMOCIONA*|APROBAD*`). Verificado E2E con curl.
2. **[HECHO] Unificar flujos duplicados de "Inscribir a Carrera"** — el diálogo cliente viejo (`InscribirCarreraDialog` en `alumnos/page.tsx`) llamaba a endpoints/campos del modelo pre-DDD que ya no existen (siempre fallaba). Se eliminó junto a sus 3 services huérfanos; la inscripción ya funciona bien desde `carreras/[id]/page.tsx` (`InscribirAlumnoDialog`, modo existente/nuevo).
3. **"Curso corto"** (ofertas de duración menor a un cuatrimestre, ej. 4 sábados) — no encaja bien en el modelo carrera/plan-céntrico actual. Deferido explícitamente por el usuario.
4. **[HECHO] `mis-comisiones` (vista PROFESOR)** — PROFESOR pasa a ser actor real (2026-07-08, supersede el punto 5 de `project_scope_solo_administrativos`). PROFESOR gana GET-only a Comisión/Cursada/MateriaPlan/Alumno/Profesor/ComisionProfesor en el Core; `/dashboard/mis-comisiones` reescrito como RSC real; `/dashboard/comisiones/[comisionId]` reusada con rama por rol (ownership check server-side, tabs Alumnos read-only + Asistencias + Calificaciones, sin tab Docentes para PROFESOR). Verificado E2E con curl + navegador (confirmado por el usuario 2026-07-08).
5. **[HECHO] Inconsistencia de patrones** — Alumnos/Profesores/Administradores migraron a Server Actions + Server Components, alineando el stack con el resto de la aplicación y eliminando `apiClient` + states del lado del cliente innecesarios.
6. **[HECHO] Error tsc preexistente / limpieza de componentes sin uso** — Se purgaron todos los componentes copiados de Radix UI/shadcn que generaban errores de TypeScript por dependencias faltantes (`input-otp`, `carousel`, `accordion`, `toast`, etc.) y que no se estaban utilizando en el proyecto. `npx tsc --noEmit` ahora finaliza sin errores.
7. **[HECHO] Gap de seguridad `ms-notas`/`ms-asistencias`** — hardening cerrado 2026-07-09. `ms-notas`/`ms-asistencias` no tienen Spring Security propio (confían en el Gateway), así que se agregó `RoleGuard.exigirRol` (`backend/commons/.../util/RoleGuard.java`, nuevo) que lee `X-User-Roles` manualmente y tira `ResponseStatusException(403)` si no matchea — reusa el `GlobalExceptionHandler` ya existente en ambos módulos. Aplicado en `CalificacionParcialController` y `AsistenciaController`: crear/listar/actualizar → ADMIN/ADMINISTRATIVO/PROFESOR; eliminar → solo ADMIN/ADMINISTRATIVO (el frontend nunca llama DELETE en estos dos recursos). No incluye ownership check (`cursadaId` ajeno) — eso sigue resuelto solo en la UI del Core, como ya estaba aceptado. `mvn compile` limpio en `commons`/`ms-notas`/`ms-asistencias`.
8. **[HECHO] Dashboard (`/dashboard`) no filtraba por rol** — `getUsuarioActual()` gatea la home: no-admin (PROFESOR) ve `DashboardNoAdmin`, ADMIN/ADMINISTRATIVO sin cambios (mismos 8 fetches y KPIs de siempre). `DashboardNoAdmin` muestra "Comisiones Asignadas" y "Alumnos Activos" (vía `getProfesorActual()` + `comisiones-profesores`/`cursadas`, ya habilitados para PROFESOR) más un acceso rápido a Mis Comisiones — sin pedir carreras/periodos/planes-estudio/inscripciones-carreras.
9. **[HECHO] Horarios de clase para PROFESOR** — cerrado 2026-07-09 en `feature/horarios-profesor`. (a) Backend verificado E2E con stack Docker completo (mysql+core+gateway+frontend), datos de prueba sembrados/borrados vía curl. (b) `HorarioClaseController.getByComisionId` ahora acepta `hasRole('PROFESOR')` además de ADMIN/ADMINISTRATIVO (`getAll`/`getById` siguen admin-only). (c) `HorarioClaseServiceImpl.calcularProximaFecha` (método puro, testeado en `HorarioClaseServiceImplTest`) traduce `diaSemana` a la próxima fecha concreta (hoy si coincide, si no dentro de los próximos 7 días); expuesto como `proximaFecha` en `HorarioClaseResponse`. UI: `/dashboard/mis-comisiones` muestra "Próxima clase: <día>, <fecha> · <hora>hs" por comisión, verificado en navegador (curl con cookie de sesión contra Docker) tras rebuildear los contenedores `backend-app`/`frontend-app` (estaban corriendo builds viejos de una sesión anterior).
10. **[REEMPLAZADO por Épica Mesas de Examen] "Próximo examen" / instancias programables** — Ver tareas de Mesas de Examen (Ítem 23).
11. **[HECHO] Promedio de Parciales dinámico + modalidad** — Columna "Promedio Parciales" en `/dashboard/comisiones/[comisionId]` (pestaña Calificaciones) recalculada dinámicamente: con 1-2 notas muestra el promedio en gris "(provisorio)" sin veredicto (el backend `CondicionCursadaService` exige los 3 parciales para dictaminar, `< 4` en un promedio parcial no implica Libre definitivo); con los 3 parciales cargados replica exactamente los umbrales de `CondicionCursadaService.calcular()` (`< 4` → badge rojo "Libre", `>= 7` y `modalidadEvaluacion=PROMOCIONAL` → badge verde "Promociona", resto → texto normal). `MateriaPlanResponse` (frontend) ganó `modalidadEvaluacion?: string` (ya lo exponía el backend desde el ítem 14, solo faltaba declararlo). Es una proyección de UI, no el estado oficial — la condición persistida sigue viniendo del botón "Cerrar cursada".
12. **[HECHO] Notas editables sin aviso** — `EditableNotaCell` ahora pide `confirm()` (valor viejo → nuevo) antes de guardar si hay `calificacionId` y ya tenía nota; cancelar revierte el input. No es inmutabilidad estricta (sigue siendo editable), solo advertencia, tal como pidió el usuario. Sin cambios de backend.
13. **[FUTURO, explícitamente diferido] Habilitación de carga de notas por ADMIN** — el usuario pidió textualmente "para futuras versiones, las notas deberán ser habilitadas por el admin para poder cargar" (2026-07-08). Es decir: un profesor no podría cargar notas de una instancia hasta que un ADMIN/ADMINISTRATIVO la habilite. No implementar ahora — el usuario lo marcó explícitamente como versión futura, no pendiente actual.
14. **[HECHO] Modalidad de evaluación por materia (PROMOCIONAL / FINAL)** — cerrado 2026-07-09/10, brainstorming + plan + implementación (13 tasks) + E2E + merge a `develop`. `MateriaPlan.modalidadEvaluacion` (enum `PROMOCIONAL|FINAL`, default `FINAL`). `Cursada.condicionFinal` pasó de `String` libre a enum `CondicionFinal{LIBRE,REGULAR,PROMOCIONADA,APROBADA}`. `CalificacionParcial` (ms-notas) ganó `tipo{PARCIAL,FINAL}` + `comisionId` (máx. 3 `PARCIAL` por cursada). `CondicionCursadaService` (Core) calcula on-demand: promedio de los 3 parciales → `<4` LIBRE, `4-6.99` REGULAR, `>=7` PROMOCIONADA solo si modalidad PROMOCIONAL (si no, sigue REGULAR); una nota `FINAL` sobre una cursada REGULAR sube a APROBADA (`>=4`) o mantiene REGULAR (`<4`); un LIBRE nunca se sobreescribe con una FINAL. Cruce entre microservicios: Core llama a ms-notas con header `X-User-Roles` (mismo mecanismo que RoleGuard); ms-notas/ms-asistencias llaman a Core con JWT interno minteado (`InternalTokenService`, mismo `JWT_SECRET` compartido) para validar fecha de parciales/asistencias contra `HorarioClase` (día de semana, `tipo=FINAL` exceptuado). Endpoints nuevos `GET /api/cursadas/{id}/condicion-preview` y `POST /api/cursadas/{id}/cerrar`. Frontend: `comisionId`/`tipo` propagados en notas/asistencias, botón "Cerrar cursada", select de modalidad en alta/edición de `MateriaPlan`. Verificado E2E con curl contra stack Docker completo (4 imágenes reconstruidas): 2 bugs reales encontrados y corregidos ahí (wiring de `CORE_API_URL`/`NOTAS_API_URL`/`JWT_SECRET` faltante en docker-compose entre microservicios; `ResponseStatusException` sin handler en Core devolvía 500 en vez de 400). Fuera de alcance explícito (ver spec): libreta de notas, mesas especiales como entidad propia, asistencia como criterio de LIBRE, historial de intentos de final.
15. **[HECHO] Alumno/Profesor creados desde la UI no podían loguearse nunca** — `UserLookupPortImpl.crearConCredencialesPorDni`/`adjuntarRol` and `UserServiceImpl.register` (legacy, sin uso en frontend) ahora habilitan `enabled=true` también para PROFESOR (antes solo ADMIN/ADMINISTRATIVO) — coincide con que PROFESOR ya tiene UI real (`mis-comisiones`). ALUMNO sigue naciendo deshabilitado (sin actor real todavía). Verificado E2E: profesor nuevo loguea sin tocar la base; alumno nuevo sigue con `ACCOUNT_DISABLED`.
16. **[HECHO] Oferta académica opt-in al crear ciclo** — mergeado a `develop` 2026-07-11, resuelve la decisión abierta "auto-disparar generación de oferta al crear ciclo". Dialog "Nuevo Ciclo Lectivo" gana checkbox "Generar oferta académica automática" (desmarcado por default) + selección de carreras (todas preseleccionadas al marcar); `createCiclo` devuelve `{id, anio}` para encadenar; el botón manual en la página del ciclo sigue existiendo (misma action compartida). Bonus fix: `generarOfertaAcademicaAutomatica` ahora idempotente en períodos (reusa "1er/2do Cuatrimestre {anio}" existentes del ciclo en vez de duplicarlos — bug latente del botón manual). No deduplica comisiones (fuera de alcance, igual que antes). Verificado E2E: curl invocando las server actions contra Docker (idempotencia confirmada, datos de prueba limpiados por SQL) + flujo completo del dialog en navegador confirmado por el usuario. Spec: `docs/superpowers/specs/2026-07-11-oferta-optin-crear-ciclo-design.md` (no versionada).
17. **[HECHO] Numeración de `docs/`** — todos los `.md` de `docs/` (excepto `superpowers/`) renombrados con código decimal `categoría.orden` (`00.00`…`05.00`, paso 10 para insertar sin renombrar). Agregado `docs/INDEX.md` con tabla de orden de lectura y convención. Links internos cruzados entre docs actualizados a los nuevos nombres. Commit `2cc886a` en `develop`, pusheado.
18. **[HECHO] Bug de Inscripción Duplicada a Carrera** — `InscripcionCarreraServiceImpl.guardar()` ahora valida con `existsByAlumnoIdAndPlanEstudioCarreraIdAndEstadoNot(alumnoId, carreraId, "BAJA")`: bloquea el alta si el alumno ya tiene inscripción vigente en cualquier plan de la misma carrera (sin importar ciclo lectivo); estado BAJA no bloquea, para permitir re-alta tras baja. Devuelve 409 con código `ALUMNO_YA_INSCRIPTO_CARRERA` (nueva `AlumnoYaInscriptoEnCarreraException` + handler en `CoreExceptionHandler`). Frontend: `inscripcion-carrera-actions.ts` propaga la descripción de error del backend y `InscribirAlumnoDialog` la muestra con toast (sonner) en vez de romper. Verificado E2E vía API contra Docker: duplicado→409, baja→re-alta OK, segundo intento→409. Rama `feature/fix-inscripcion-duplicada`.
19. **[HECHO] Comportamiento ambiguo de Ciclo Lectivo "Dado de baja"** — Se resolvió el diseño (opción 1): Solo el perfil ADMIN puede ver los ciclos inactivos en el dashboard (para poder reactivarlos editándolos), el resto de perfiles no los ven. Ningún usuario puede navegar hacia el detalle de un ciclo inactivo (el Link se desactiva). Se agregó un toggle "Ciclo Activo" en el modal de edición (`EditarCicloDialog`) que permite reactivarlo y envía la bandera `activo` al backend. En el backend, `CicloLectivoServiceImpl.actualizar` ahora acepta y guarda el nuevo estado de `activo` si viene en el request.
20. **[HECHO] Bug de Cursada Duplicada (Comisiones)** — `CursadaServiceImpl.guardar()` no validaba la existencia previa. Se agregó validación `existsByAlumnoIdAndComisionId` que dispara una nueva `AlumnoYaInscriptoEnComisionException` (409 Conflict). En Frontend, `AgregarAlumnoDialog` fue mejorado: las notificaciones ahora usan `sonner` (toasts en lugar de `alert`) y el selector de alumnos oculta dinámicamente a los alumnos que ya están inscriptos en la tabla, previniendo el error a nivel UI.
21. **[REEMPLAZADO por Épica Mesas de Examen] Examen Final en condición Libre** — Ver tareas de Mesas de Examen (Ítem 23).
22. **[DEUDA TÉCNICA - Arquitectura] Desacoplar comunicación sincrónica (Core <-> Microservicios)** — *(Baja prioridad)*. Actualmente existe un acoplamiento y dependencia circular en tiempo de ejecución (HTTP REST): el Core depende de `ms-notas` para cerrar cursadas, y los MS dependen del Core para validar horarios. A futuro, evaluar migrar a una comunicación asincrónica orientada a eventos (ej. RabbitMQ/Kafka) para mejorar la resiliencia y el aislamiento.
23. **[DEUDA TÉCNICA - Testing] Flaky test en security:** `UserAdminServiceImplTest.crear_asignaAmbosRoles_siSeSeleccionanAdminYAdministrativoJuntos` falla aleatoriamente por el orden no determinístico al iterar el `Set<Rol>`.
24. **[HECHO] [ÉPICA] Mesas de Examen (Instancias Especiales)** — Rediseño para separar los exámenes finales de la cursada regular (ver `docs/superpowers/specs/2026-07-14-mesas-examen-design.md`). Reemplaza la funcionalidad parcheada en el ítem 14. Las 5 fases completadas 2026-07-14/15 (ramas `feature/epica-mesas-fase1`→`fase5`, encadenadas; mergear fase5 integra todo). **Verificado E2E contra Docker 2026-07-15** (stack completo rebuildeado, script Python vía API Gateway): crear mesa con tribunal, rechazo de tribunal sin rol PROFESOR (400), filtro `?tribunalUserId=`, inscripción + duplicado (409), login de profesor real, carga/actualización de nota con libro/folio (POST/PUT `/api/notas/mesas`), ownership 404 en `/dashboard/mis-mesas/[id]` para mesas ajenas, flujo viejo de parciales/`condicion-preview` intacto tras la limpieza de `tipo`. Bug encontrado y corregido en la verificación: `PeriodoAcademicoController` (GET listar/porId) no incluía `hasRole('PROFESOR')` — sin el fix, `/dashboard/mis-mesas` mostraba "Período #N" en vez del nombre real para los profesores (403 silencioso vía `fetchCore`). Nota de entorno (2026-07-15, resuelta): `APP_PROFILE=local` usaba `ddl-auto: create-drop`, perdiendo datos en cada rebuild/restart de `backend-app`. Cambiado a `ddl-auto: update` en `application-local.yml` (verificado: sobrevive a un `docker restart`). Flyway sigue deshabilitado en local y el baseline `V1__init.sql` sigue pre-DDD — regenerarlo queda como deuda técnica aparte (ver ítem 22), evaluar a futuro si conviene habilitarlo también en local.
    - [x] **Fase 1: Refactor del Modelo Base y Limpieza.** Hecho en Core: `CondicionCursadaService.calcular()` ya no evalúa notas `FINAL` — devuelve solo LIBRE/REGULAR/PROMOCIONADA y `notaCierre` = promedio de parciales. Tests de finales eliminados; nuevo test verifica que las notas `FINAL` se ignoran. Pendiente deliberado: el `tipo=FINAL` en `ms-notas` (`TipoInstancia`, validaciones) queda intacto hasta Fase 3 para no romper el frontend actual. Rama `feature/epica-mesas-fase1`.
    - [x] **Fase 2: Backend Core - Mesas.** Entidades `MesaExamen` (materiaPlan, periodoAcademico, fechaHora, estado PROGRAMADA/CERRADA, tribunal N:M con `User` rol PROFESOR vía tabla `mesa_examen_tribunal`) e `InscripcionMesa` (mesa, alumno `User` rol ALUMNO, condición REGULAR/LIBRE/PROMOCION, fechaInscripcion). Repos + `MesaExamenService` (crear, listar, buscarPorId, inscribirAlumno con 409 `ALUMNO_YA_INSCRIPTO_MESA` si duplicado, listarInscripciones) + `MesaExamenController` (`/api/mesas-examen`, RBAC igual que cursadas). Sin Flyway (decisión con usuario 2026-07-14): tablas las crea Hibernate `ddl-auto` como el resto del modelo DDD — el baseline `V1__init.sql` quedó pre-DDD y Flyway está deshabilitado en el entorno Docker actual (perfil `local`); regenerar baseline = deuda aparte. 4 tests unitarios. Rama `feature/epica-mesas-fase2`.
    - [x] **Fase 3: Backend Notas.** Nueva `CalificacionMesa` en ms-notas (mesaExamenId, alumnoId, nota nullable, ausente default false, libro/folio opcionales) + repo + service + `CalificacionMesaController` en `/api/notas/mesas` (GET por mesaExamenId, POST, PUT `/{id}`; RoleGuard ADMIN/ADMINISTRATIVO/PROFESOR; duplicado alumno+mesa → 409). Ruta `/api/notas/**` agregada al gateway (predicate `notas-api`). Limpieza completada (pendiente de Fase 1): eliminado `TipoInstancia` y el campo `tipo` de `CalificacionParcial`/request — máx. 3 notas por cursada y validación de fecha aplican siempre; seeder ajustado. Espejo en Core: `CalificacionParcialDto.tipo` eliminado y `CondicionCursadaService` ya no filtra por tipo (evita romper el cierre de cursadas). ⚠ Frontend viejo que mande `tipo` o cree finales por comisión queda roto hasta Fase 4/5 (aceptado). Rama `feature/epica-mesas-fase3`.
    - [x] **Fase 4: Frontend Administrativo.** `/dashboard/mesas-examen` (RSC): tabla Materia/Fecha/Estado/Tribunal (nombres resueltos vía `/materias-plan` y `/profesores` por `userId`) + `NuevaMesaDialog` (materia, período, datetime-local, tribunal por checkboxes de profesores activos → manda `userId`s) con action `createMesaExamen`. Detalle `/dashboard/mesas-examen/[id]` (RSC): datos de la mesa, card de tribunal, tabla de inscriptos (nombre/DNI/condición/fecha) + `InscribirAlumnoMesaDialog` (oculta alumnos ya inscriptos, manda `userId`, condición REGULAR/LIBRE/PROMOCION) con action `inscribirAlumnoEnMesa`; toasts sonner. Sidebar: "Mesas de Examen" en Gestión Académica (solo ADMIN/ADMINISTRATIVO). Limpieza `tipo`: eliminado de `nota-actions` (payloads a ms-notas), `EditableNotaCell` (prop), y de la page de comisión (interface + filtros `tipo==="PARCIAL"` que hubieran roto promedios con la respuesta nueva sin `tipo`). Los selects de modalidad FINAL/PROMOCIONAL de MateriaPlan NO se tocaron (concepto distinto, sigue vigente). `tsc --noEmit` limpio. Rama `feature/epica-mesas-fase4`.
    - [x] **Fase 5: Frontend Profesor.** Backend: `GET /api/mesas-examen?tribunalUserId=` (filtro opcional, `findByTribunalId` sobre la N:M) — el listado completo sigue disponible para los 3 roles, el filtro es UX. Frontend: `/dashboard/mis-mesas` (RSC, tabla Materia/Período/Fecha/Estado sin botón crear, alimentada con el `userId` de `getProfesorActual()` — que ahora expone `userId`) y `/dashboard/mis-mesas/[id]` con ownership check server-side (404 si el profesor no integra `tribunalIds`, mismo criterio que mis-comisiones) + tabla de inscriptos con `CeldasNotaMesa` (nota con confirm al sobrescribir + libro/folio, guardado onBlur) → action `saveCalificacionMesa` (POST/PUT `/api/notas/mesas` de ms-notas). Sidebar: "Mis Mesas de Examen" solo PROFESOR. Ownership en ms-notas sigue solo-UI (coherente con lo aceptado en ítem 7 para parciales/asistencias). Rama `feature/epica-mesas-fase5`.
25. **[HECHO] Crasheos por Server Actions sin Try/Catch:** `EditableNotaCell`, `TomarAsistenciaDialog` y `NuevaInstanciaDialog` ahora atrapan el error con `try/catch` + `toast.error(error.message)` (sonner) en vez de crashear el árbol. En los dialogs, `reset()`/`setOpen(false)` solo corren si la Server Action tuvo éxito, más `toast.success` al cerrar bien. `isSaving` sigue apagándose en `finally` en `EditableNotaCell`. Rama `fix/server-actions-error-handling`, mergeada a `develop` (commit `8eec6c5`).
26. **[HECHO] Relajar validación de fechas para Notas Parciales:** `CalificacionParcialService` (ms-notas) ya no valida `fecha` contra los días de clase de la comisión — se eliminó `validarFechaDeClase` y su llamada en `crear`/`actualizar`, junto con la dependencia inyectada `HorarioClient`. Tests `rechazaParcialConFechaQueNoEsDiaDeClase`/`permiteParcialConFechaQueSiEsDiaDeClase` eliminados; `rechazaElCuartoParcialDeLaMismaCursada`/`permiteElTercerParcial` siguen pasando. `mvn compile`/`test` limpio en `ms-notas`. Nota: `HorarioClient` (cliente HTTP a Core para consultar `HorarioClase`) queda sin ningún caller en el módulo tras este cambio — dead code, no se borró (fuera del pedido explícito); `CalificacionParcial.fecha` en el modelo/DTO tampoco se tocó, sigue existiendo pero ya no se valida contra horarios (los finales de Mesas de Examen nunca usaron este validador, ver ítem 24).
27. **[DEUDA TÉCNICA - Frontend] UI para Horarios de Clase:** El backend exige horarios para validar la asistencia, pero no existe interfaz para que el Administrador asigne módulos horarios a una comisión recién creada. Se requiere crear una pestaña "Horarios" en el detalle de la comisión (`/dashboard/comisiones/[comisionId]`).

---

## Histórico (modelo pre-refactor DDD, ya superado)

## 🔴 CRÍTICO / URGENTE
- [x] **Bug 401 en Frontend (CSR):** El token JWT no se inyecta correctamente en el cliente (`api-client.ts`) durante la navegación, provocando 401 Unauthorized, cierres de sesión forzados y pérdida de datos en los formularios (ej: crear Profesor).

## 🟠 Deuda Técnica y Reglas de Negocio (Backend)
- [x] **Sistema de migraciones de schema (Flyway):** Adoptado (`flyway-core`+`flyway-mysql`, `ddl-auto: validate`, `V1__init.sql` generado por dump del DDL real de Hibernate). Se estandarizó el desarrollo en MySQL único (se eliminó el perfil `dev`/H2 in-memory). Ver `docs/superpowers/specs/2026-07-05-unificacion-actores-flyway-design.md`.
- [x] **Automatización de Legajos (Todos los actores):** Al dar de alta a cualquier actor (alumno, profesor, administrativo), el sistema autogenera su número de legajo (formato `AAAA-DNI`) en `User.legajo`, en lugar de solicitarlo manualmente en el frontend.
- [x] **Unificación de Actores y Multi-rol (Legajo Único):** `User` es la entidad única con `legajo` centralizado (todos los roles). Alta de un rol nuevo sobre un DNI ya existente (`GET /api/personas/dni/{dni}` + `UserLookupPort.crearConCredencialesPorDni`) adjunta el rol al `User` existente en vez de duplicarlo (409 `ROL_YA_ASIGNADO` si el rol ya estaba asignado). Frontend auto-detecta la persona existente al tipear el DNI en los 3 formularios de alta.
- [x] **Filtrado de Datos Server-Side (Punto 3):** 
  - [x] **ms-asistencias:** Agregar RequestParams opcionales (`?comisionId=X&fecha=Y`) en Controlador, Servicio y Repositorio.
  - [x] **ms-notas:** Agregar RequestParams de filtrado (ej. `?comisionId=X` en Examen y `?examenId=X` en Nota) en controladores, modificando sus respectivos Servicios y Repositorios.

## 🟡 Tareas de UX y Frontend
- [x] **Landing Page:** Eliminar el panel de 'Buscar alumno' (no tiene sentido de negocio sin auth). Reemplazar por un mensaje de bienvenida y un botón de 'Ingresar al Sistema'.
- [x] **Login:** Añadir un enlace o botón para regresar a la página de inicio (Landing) o hacer clickeable el logo.
- [x] **Dashboard (Datos Mockeados):** Ocultar las tarjetas de KPIs temporalmente, o desarrollar un endpoint real en el backend (`/api/core/dashboard/metricas`).
- [x] **Sidebar (Menú Inactivo):** Ocultar temporalmente 'Materias', 'Reportes', 'Certificados' y 'Configuración' para evitar clics inútiles y errores 404 de prefetch.
- [x] **Gestión de Administradores:** Desarrollar una pantalla/módulo para crear o gestionar usuarios con rol 'ADMIN' o 'ADMINISTRATIVO'.
- [x] **Selector de Perfil (Context Switcher):** Implementado en el Navbar para usuarios con múltiples roles (cambia modo de vista sin re-loguearse).
- [x] **Bugfix Alumnos (Teléfonos):** Se sacó el `@UniqueConstraint` de `telefono` en `User.java` (repetible para todos los roles). `telefonoSecundario` opcional en Alumno.
- [x] **Formulario Profesor:** Etiquetas de teléfonos renombradas ('Teléfono Personal' y 'Teléfono Secundario'), segundo teléfono opcional.
- [x] **Restricción de Edición de Perfiles:** El backend ya restringía `actualizar` a `ADMIN`/`ADMINISTRATIVO` en los 3 controllers (Alumno/Profesor/Administrador) — PROFESOR nunca pudo editar vía API. Se agregó el guard `RequireRole` en frontend (3 páginas de gestión) para que, si un PROFESOR navega directo por URL (el sidebar ya no le muestra el link), vea "No tenés permiso" en vez de botones que fallarían con 403.
- [x] **Legajo de solo lectura:** El legajo en sí no es editable directamente en ningún formulario — se recalcula automáticamente solo como efecto del cambio de DNI (ver ítem siguiente).
- [x] **Recalcular legajo si cambia el DNI:** `UserLookupPort.actualizarDniSiCambio` — al editar el DNI de un actor existente (ahora editable en los 3 formularios), recalcula el legajo (`AñoActual-DNI`) y libera el DNI/legajo/username viejos (username solo se sincroniza si seguía la convención `username=DNI` de alta; usernames personalizados como el seed ADMIN quedan intactos). Valida que el DNI nuevo no esté en uso. Cubierto con tests unitarios en `UserLookupPortImplTest`.
- [x] **Completar campos editables en Formularios:** Nombre/Apellido/Email ahora editables en los 3 diálogos de edición (Alumno/Profesor/Administrador — este último ya lo tenía). Backend: `AlumnoUpdateRequest`/`ProfesorUpdateRequest` ganan los 3 campos; `AlumnoServiceImpl`/`ProfesorServiceImpl` los mapean a `User` vía `userLookupPort.actualizarEmailSiCambio` (valida email único, mismo patrón que el DNI) + `setNombre`/`setApellido` directo.
- [x] **Multi-rol simultáneo para Administradores:** Un usuario puede tener ADMIN y ADMINISTRATIVO a la vez (ej. super-usuario para el director). `rol: Rol` → `roles: Set<Rol>` en `CrearAdministradorRequest`/`ActualizarAdministradorRequest`/`UsuarioAdminResponse`. `UserAdminServiceImpl.crear()` itera los roles pedidos reusando `crearConCredencialesPorDni` (1ª llamada crea, siguientes adjuntan rol vía el mecanismo ya existente). Frontend: botones Admin/Administrativo pasan de exclusivos a checkboxes independientes; la lista muestra un badge por rol.

---
*Nota: La arquitectura base, el API Gateway, la seguridad JWT y los microservicios core ya se encuentran desarrollados (Fase 1 y 2 completadas).*

## 🟡 Mejoras de UX a implementar
- [x] **Buscador de Materias en Dialogs:** `SelectMateriaBuscable` (`frontend/components/materias/select-materia-buscable.tsx`) — `Input` de búsqueda + lista scrolleable de radios (mismo patrón visual que correlativas/tribunal), filtro sin acentos (`lib/texto.ts`), opción seleccionada siempre visible aunque no matchee el filtro, valor real viaja en un `<input type="hidden">` para Server Actions. Integrado en `NuevaMesaDialog`, `AgregarMateriaDialog` (vía `opcionesExtra` con botón "+ Crear materia nueva") y `EditarMateriaPlanDialog`. Como `hidden required` no bloquea el submit nativo, se agregó guard manual (`if (!materiaId) toast.error(...)`) en los 3 `handleSubmit`.
