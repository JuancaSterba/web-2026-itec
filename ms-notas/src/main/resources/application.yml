spring:
  application:
    name: ms-notas
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

server:
  port: 8084

logging:
  level:
    root: info

jwt:
  secret: ${JWT_SECRET:tu-clave-muy-segura-de-32-caracteres-o-mas-1234}

core:
  api:
    url: ${CORE_API_URL:http://localhost:8082}
